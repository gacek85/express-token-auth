{
  "version": "0.0.1",
  "description": "Express JS simple, token based, authorization middleware",
  "license": "MIT",
  "name": "auth-token",
  "repository": "gacek85/express-token-auth",
  "private": false,
  "tags": [
    "express",
    "middleware",
    "authorization",
    "token"
  ],
  "engines": {
    "node" : ">= 4.9"
  },
  "scripts": {
    "test": "env env=test mocha --recursive"
  },
  "dependencies": {
    "lodash": "^4.15.0",
    "crypto": "^0.0.3"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.0.2",
    "sinon": "^1.17.5",
    "sinon-chai": "^2.8.0"
  },
  "config": {
    "blanket": {
      "pattern": "app",
      "data-cover-never": "node_modules"
    }
  }
}